const sum = function (a, b) {
  return a + b;
};
const subtraction = function (a, b) {
  return a - b;
};
const mul = function (a, b) {
  return a * b;
};
const division = function (a, b) {
  return a / b;
};

const userInput1 = prompt('введите 1 число');
const userInput2 = prompt('введите 2 число');
const operation = prompt('Введите знак операции');

const calculate = function (a, b, sign) {
  if (!isNaN(userInput1)) {
    if (!isNaN(userInput2)) {
      /* 
      YOUR CODE ARE HERE 
      */
    }
  }
};

const result = calculate( /* передать сюда 2 числа и знак операции */ );

console.log(result);
